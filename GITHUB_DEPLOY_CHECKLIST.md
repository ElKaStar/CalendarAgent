# Чеклист для загрузки на GitHub и развертывания на Ubuntu

## ✅ Что уже готово

1. **Кроссплатформенность кода**
   - ✅ Все пути используют `os.path.join()` - работает на Windows и Linux
   - ✅ Нет хардкода Windows-специфичных путей
   - ✅ Используется `pathlib.Path` где необходимо

2. **Git конфигурация**
   - ✅ `.gitattributes` настроен (LF для всех файлов, CRLF только для .bat/.ps1)
   - ✅ `.gitignore` настроен правильно (игнорирует .env, service-account.json, логи, БД)
   - ✅ `run_bot.sh` создан для Linux
   - ✅ `DEPLOY_UBUNTU.md` - полная инструкция по развертыванию

3. **Зависимости**
   - ✅ `requirements.txt` содержит все необходимые пакеты
   - ✅ ffmpeg будет установлен через apt на Ubuntu

## ⚠️ Что нужно сделать перед загрузкой на GitHub

### 1. Проверьте секретные данные

Убедитесь, что в `.gitignore` есть:
- `.env` - переменные окружения
- `service-account.json` - Google credentials
- `*.log` - логи (могут содержать токены)
- `*.db` - базы данных

### 2. Создайте `.env.example` (если еще нет)

Убедитесь, что `env.example` содержит все необходимые переменные БЕЗ реальных значений.

### 3. Проверьте файлы перед коммитом

```bash
# Проверка статуса
git status

# Просмотр изменений
git diff --cached
```

### 4. Создайте первый коммит

```bash
git commit -m "Initial commit: Calendar Agent Telegram Bot"
```

### 5. Создайте репозиторий на GitHub и загрузите

```bash
# Добавьте remote (замените на ваш URL)
git remote add origin https://github.com/ваш_username/CalendarAgent.git

# Загрузите код
git push -u origin master
```

## 🐧 Развертывание на Ubuntu 22.04

Следуйте инструкции в `DEPLOY_UBUNTU.md`. Кратко:

1. Установите зависимости: `python3`, `pip`, `ffmpeg`
2. Клонируйте репозиторий
3. Создайте виртуальное окружение и установите зависимости
4. Настройте `.env` и загрузите `service-account.json`
5. Запустите через systemd (инструкция в DEPLOY_UBUNTU.md)

## 🔍 Потенциальные проблемы и решения

### Проблема: Окончания строк (LF/CRLF)
**Решение:** ✅ Уже решено через `.gitattributes`

### Проблема: Пути в логах показывают обратные слеши
**Решение:** Это только косметическая проблема в логах. Код использует `os.path.join()`, который работает правильно на всех платформах.

### Проблема: ffmpeg не найден на Ubuntu
**Решение:** 
```bash
sudo apt install -y ffmpeg
```

### Проблема: Whisper модель не скачивается
**Решение:** Убедитесь, что есть интернет и достаточно места (~1.5GB для base модели)

### Проблема: Права доступа
**Решение:**
```bash
chmod +x run_bot.sh
chown -R ваш_пользователь:ваш_пользователь ~/CalendarAgent
```

## 📝 Важные заметки

1. **Не коммитьте секретные данные** - проверьте `.gitignore`
2. **На Ubuntu используйте `python3`** вместо `python`
3. **Whisper скачает модель автоматически** при первом запуске (~1.5GB)
4. **Используйте systemd** для автозапуска в production
5. **Все пути кроссплатформенные** - код будет работать на Ubuntu без изменений

## ✅ Финальная проверка перед push

- [ ] `.env` не в Git (проверьте `git status`)
- [ ] `service-account.json` не в Git
- [ ] Все логи и БД в `.gitignore`
- [ ] `requirements.txt` актуален
- [ ] `DEPLOY_UBUNTU.md` содержит правильные инструкции
- [ ] `run_bot.sh` имеет права на выполнение (на Ubuntu: `chmod +x`)

