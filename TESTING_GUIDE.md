# Руководство по тестированию дневника питания

## ✅ Бот запущен и готов к работе

Все модули успешно загружены, база данных инициализирована.

## Тестирование в Telegram

### 1. Тест определения интента

Отправьте боту сообщения и проверьте, что они правильно определяются:

**Календарь (должно создать событие):**
- "Завтра в 15:00 встреча с Катей"
- "Послезавтра в 10:00 созвон с командой"
- "Запиши меня завтра на маникюр на 3 часа дня"

**Еда (должно сохранить в дневник питания):**
- "Еда: завтрак омлет и кофе"
- "Съела салат цезарь и капучино"
- "Меню за день: утром овсянка; днем борщ и хлеб; вечером рыба и овощи"
- "Перекус: яблоко, йогурт"
- "Вчера: паста и салат"

### 2. Тест команд дневника питания

Проверьте команды:

- `/food_help` - должна показать справку
- `/food_today` - должна показать записи за сегодня (если есть)
- `/food_day 2024-11-26` - должна показать записи за указанную дату
- `/food_last 5` - должна показать последние 5 записей
- `/food_sum 2024-11-26` - должна показать сводку за день
- `/food_export 2024-11-26` - должна отправить CSV файл с данными

### 3. Тест голосовых сообщений

Отправьте голосовое сообщение с записью о еде:
- "Еда: завтрак омлет и кофе"
- "Съела салат цезарь"

Бот должен:
1. Распознать речь через STT
2. Определить интент (еда)
3. Сохранить в дневник питания

### 4. Тест приоритета календаря

Отправьте сообщение, которое может быть и календарём, и едой:
- "Завтра в 10:00 завтрак с командой"

Должно создать событие в календаре (календарь имеет приоритет).

## Проверка логов

Логи сохраняются в файл `bot.log`. Проверьте, что видны записи:
- "Определён календарный интент для текста: ..."
- "Определён food интент для текста: ..."
- "Сообщение направлено в FoodPipeline: ..."
- "Сообщение направлено в CalendarPipeline: ..."
- "Сохранена запись о еде: ID=..."

## Проверка базы данных

База данных находится в файле `events.db`. Вы можете проверить таблицу `food_logs`:

```sql
SELECT * FROM food_logs ORDER BY created_at DESC LIMIT 10;
```

## Ожидаемое поведение

### Успешная запись о еде

После отправки "Еда: завтрак омлет и кофе" бот должен ответить:

```
✅ Записала в дневник питания:

📅 Дата: 2024-11-26
🍽 Приём пищи: Завтрак
📝 Продукты: Омлет, Кофе
```

### Команда /food_today

Если есть записи за сегодня, бот покажет:

```
🍽 Дневник питания за сегодня (2024-11-26):

• Завтрак: Омлет, Кофе
  📝 Еда: завтрак омлет и кофе...

• Обед: Борщ, Хлеб
  📝 Меню за день: утром овсянка; днем борщ...
```

## Возможные проблемы

### Бот не отвечает

1. Проверьте, что бот запущен: `python bot.py`
2. Проверьте логи: `Get-Content bot.log -Tail 20`
3. Проверьте, что токен Telegram Bot правильный в `.env`

### Сообщения не определяются как еда

1. Проверьте логи - должно быть "Определён food интент"
2. Убедитесь, что в сообщении есть ключевые слова о еде
3. Попробуйте явно указать: "Еда: ..."

### Ошибки при сохранении в БД

1. Проверьте права доступа к файлу `events.db`
2. Проверьте логи на ошибки SQLite
3. Убедитесь, что таблица `food_logs` создана

## Результаты автоматического тестирования

Все автоматические тесты пройдены успешно:

✅ Импорты модулей
✅ Определение интента
✅ Парсинг еды
✅ Работа с БД

## Следующие шаги

После тестирования вы можете:
1. Добавить больше продуктов в словарь парсера
2. Улучшить определение типа приёма пищи
3. Добавить LLM-режим парсинга для более точного извлечения данных
4. Добавить статистику по калориям (если есть база данных калорийности)

