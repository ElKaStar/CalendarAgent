# 🤖 Calendar Agent - Telegram-бот для управления Google Calendar

Telegram-бот для управления Google Календарём через голосовые и текстовые сообщения с использованием GigaChat для парсинга естественного языка. Также включает дневник питания и локальное распознавание речи через Whisper.

## 🎯 Возможности

### 📅 Календарь
- ✅ Создание событий в Google Calendar через естественный язык
- 🔔 Автоматические напоминания перед событиями
- 📋 Просмотр ближайших событий
- ❌ Отмена событий
- 🤖 Умный парсинг дат и времени через GigaChat

### 🎤 Распознавание голоса
- 🎙️ **Локальное распознавание речи через Whisper** (не требует внешних API)
- 🔊 Поддержка голосовых сообщений в форматах OGG, MP3, WAV
- 🔄 Автоматическая конвертация аудио через ffmpeg
- ✨ Нормализация распознанного текста через GigaChat
- 🚀 Модель Whisper кэшируется для быстрой работы

### 🍽️ Дневник питания
- 📝 Запись приёмов пищи (завтрак, обед, ужин, перекус)
- 🗓️ Просмотр записей за день, неделю или период
- 📊 Сводки и статистика по питанию
- 📤 Экспорт данных в CSV
- 🔍 Автоматическое определение интента (еда vs календарь)
- 🎯 Кодовые слова для гарантированной записи в дневник

## 📋 Требования

- Python 3.10+
- **ffmpeg** (для обработки голосовых сообщений)
- Telegram Bot Token (от @BotFather)
- GigaChat API credentials
- Google Calendar API (Service Account)

### Установка ffmpeg

**Windows:**
- Скачайте с https://ffmpeg.org/download.html
- Добавьте в PATH или поместите в папку проекта

**Linux (Ubuntu/Debian):**
```bash
sudo apt-get update
sudo apt-get install ffmpeg
```

**macOS:**
```bash
brew install ffmpeg
```

**Проверка:**
```bash
ffmpeg -version
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

**Примечание:** При первом запуске Whisper автоматически скачает модель `base` (~1.5GB). Это произойдёт один раз, модель будет кэширована.

### 2. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните:

```bash
cp .env.example .env
nano .env
```

### 3. Настройка Google Calendar

#### Создание Service Account:

1. Перейдите на https://console.cloud.google.com/
2. Создайте новый проект (например, "Calendar Bot")
3. Включите Google Calendar API
4. Создайте Service Account:
   - APIs & Services → Credentials
   - Create Credentials → Service Account
   - Укажите имя (например, "calendar-bot")
5. Скачайте JSON-ключ:
   - Найдите созданный Service Account
   - Keys → Add Key → Create new key → JSON
   - Сохраните как `service-account.json`
6. Дайте доступ к календарю:
   - Откройте скачанный JSON и скопируйте `client_email`
   - Откройте Google Calendar
   - Настройки календаря → Настройки доступа
   - Добавьте email Service Account с правами "Вносить изменения в мероприятия"

### 4. Получение GigaChat credentials

1. Перейдите на https://developers.sber.ru/portal/products/gigachat
2. Зарегистрируйтесь и создайте приложение
3. Получите `CLIENT_ID` и `CLIENT_SECRET`
4. Укажите их в `.env`

### 5. Создание Telegram бота

1. Найдите @BotFather в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен в `.env`

### 6. Запуск

```bash
python bot.py
```

Или в фоне (Linux):

```bash
nohup python bot.py > bot.log 2>&1 &
```

## 💬 Использование

### Команды бота:

#### Календарь:
- `/start` - начало работы и инструкции
- `/help` - справка по использованию
- `/list` - показать ближайшие 5 событий
- `/cancel <название>` - отменить событие

#### Дневник питания:
- `/food_help` - справка по дневнику питания
- `/food_today` - что записано за сегодня
- `/food_day YYYY-MM-DD` - лог за указанную дату
- `/food_last N` - последние N записей (по умолчанию 10)
- `/food_sum YYYY-MM-DD` - сводка за день
- `/food_delete ID` - удалить запись по ID
- `/food_export YYYY-MM-DD` - экспорт данных за дату в CSV

### Примеры запросов для календаря:

**Текстовые сообщения:**
```
Завтра в 15:00 встреча с Катей по ипотеке, час
Послезавтра в 10:00 созвон с командой, 30 минут, онлайн
В пятницу в 18:00 ужин с друзьями
Через неделю планёрка с 9:00 до 10:30
```

**Голосовые сообщения:**
- Отправьте голосовое сообщение с описанием события
- Бот автоматически распознаёт речь через Whisper
- Текст нормализуется и создаётся событие в календаре

### Примеры запросов для дневника питания:

**С кодовым словом (гарантированная запись):**
```
menu овсянка 200 грамм
меню творог 40г
menu завтрак: омлет и кофе
```

**Без кодового слова (автоматическое определение):**
```
Еда: завтрак омлет и кофе
Съела салат цезарь и капучино
Перекус: яблоко, йогурт
Вчера: паста и салат
Меню за день: утром овсянка; днем борщ и хлеб; вечером рыба и овощи
```

**Голосовые сообщения:**
- Отправьте голосовое сообщение с описанием еды
- Бот распознаёт речь и автоматически определит интент (еда или календарь)

### Ответ бота (календарь):

```
✅ Создала событие:

📌 Встреча с Катей по ипотеке
🕐 16.01.2024 15:00 - 16:00

⏰ Напомню за 15 минут
```

### Напоминание:

```
⏰ Напоминание!

📌 Встреча с Катей по ипотеке
🕐 16.01.2024 в 15:00

Через 15 минут
```

### Ответ бота (дневник питания):

```
✅ Записано в дневник питания:

🍽️ Завтрак
📅 2024-01-16
🥗 Омлет, Кофе

ID записи: 123
```

### Просмотр записей:

```
📊 Дневник питания за 2024-01-16:

🍽️ Завтрак (08:30)
  • Омлет
  • Кофе

🍽️ Обед (13:00)
  • Борщ
  • Хлеб

🍽️ Ужин (19:00)
  • Рыба
  • Овощи

Всего записей: 3
```

## 🔧 Конфигурация

Все настройки в `.env`:

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | - |
| `GIGACHAT_CLIENT_ID` | GigaChat Client ID | - |
| `GIGACHAT_CLIENT_SECRET` | GigaChat Client Secret | - |
| `GIGACHAT_SCOPE` | GigaChat Scope | `GIGACHAT_API_PERS` |
| `GOOGLE_CREDENTIALS_FILE` | Путь к Service Account JSON | `service-account.json` |
| `GOOGLE_CALENDAR_ID` | ID календаря | `primary` |
| `TIMEZONE` | Временная зона | `Europe/Moscow` |
| `REMINDER_MINUTES_BEFORE` | Минут до напоминания | `15` |
| `REMINDER_CHECK_INTERVAL` | Интервал проверки (сек) | `60` |
| `DATABASE_FILE` | Файл БД SQLite | `events.db` |
| `TEMP_DIR` | Директория для temp файлов | `temp` |
| `STT_PROVIDER` | Провайдер STT: `whisper` или `yandex` | `whisper` |
| `WHISPER_MODEL` | Модель Whisper: `tiny`, `base`, `small`, `medium`, `large` | `base` |
| `STT_API_KEY` | Яндекс SpeechKit API ключ (если используется) | - |
| `STT_FOLDER_ID` | Яндекс SpeechKit Folder ID (если используется) | - |

**Примечание:** По умолчанию используется локальное распознавание через Whisper (не требует внешних API). Модель `base` - оптимальный баланс скорости и качества для CPU.

## 🐛 Troubleshooting

### Бот не отвечает

- Проверьте `TELEGRAM_BOT_TOKEN`
- Проверьте логи: `tail -f bot.log`
- Убедитесь, что бот запущен: `ps aux | grep bot.py`

### Ошибка аутентификации GigaChat

- Проверьте `GIGACHAT_CLIENT_ID` и `GIGACHAT_CLIENT_SECRET`
- Убедитесь, что scope правильный: `GIGACHAT_API_PERS`

### Не создаются события в Google Calendar

- Проверьте, что Service Account email добавлен в календарь
- Проверьте путь к `service-account.json`
- Убедитесь, что Google Calendar API включён

### Не приходят напоминания

- Проверьте, что `reminder_worker` запущен (смотри логи)
- Проверьте `REMINDER_CHECK_INTERVAL`
- Проверьте БД: `sqlite3 events.db "SELECT * FROM events;"`

### Ошибка распознавания речи (Whisper)

- **Проверьте установку ffmpeg:** `ffmpeg -version`
- **Проверьте установку Whisper:** `python -c "import whisper; print(whisper.__version__)"`
- **Проблема с загрузкой модели:**
  - Убедитесь, что есть интернет (первая загрузка ~1.5GB)
  - Проверьте место на диске (~1.5GB для модели `base`)
  - Проверьте права доступа к `~/.cache/whisper/`
- **Медленное распознавание:**
  - Это нормально для CPU (5-15 секунд для коротких сообщений)
  - Модель `base` - компромисс между скоростью и качеством
- **Неправильное распознавание:**
  - Говорите чётко, без фонового шума
  - Whisper лучше работает с качественным аудио
  - Ошибки распознавания автоматически исправляются для кодовых слов дневника питания

**Альтернатива:** Можно использовать Яндекс SpeechKit, указав в `.env`:
```env
STT_PROVIDER=yandex
STT_API_KEY=ваш_ключ
STT_FOLDER_ID=ваш_folder_id
```

## 📁 Структура проекта

```
CalendarAgent/
├── bot.py                  # Основной файл приложения
├── stt_whisper.py         # Модуль распознавания речи через Whisper
├── requirements.txt        # Зависимости Python
├── .env.example           # Пример конфигурации
├── .env                   # Ваша конфигурация (не в git)
├── service-account.json   # Google Service Account (не в git)
├── events.db             # SQLite база данных (создаётся автоматически)
├── bot.log               # Логи приложения
├── temp/                 # Временные файлы (голосовые)
├── features/             # Модули функциональности
│   └── food/             # Дневник питания
│       ├── config.py     # Конфигурация (кодовые слова)
│       ├── intent_router.py  # Определение интента (еда vs календарь)
│       ├── food_nlu.py   # Парсинг сообщений о еде
│       ├── food_db.py    # Работа с БД
│       ├── food_handlers.py  # Обработчики команд
│       └── README.md      # Документация модуля
├── tests/                # Тесты
│   └── test_whisper.py   # Тесты Whisper
├── PRD.md               # Полная документация
├── WHISPER_SETUP.md     # Инструкция по настройке Whisper
├── DEPLOY_UBUNTU.md     # Инструкция по развертыванию на Ubuntu
└── README.md            # Этот файл
```

## 📝 Логи

Логи пишутся в:
- `bot.log` - файл с историей
- `stdout` - консоль

Уровни логирования:
- `INFO` - создание событий, напоминания, команды
- `WARNING` - повторные попытки, некритичные ошибки
- `ERROR` - ошибки API, парсинга, сетевые ошибки

## 🔒 Безопасность

- ⚠️ Не коммитьте `.env` и `service-account.json` в git
- ⚠️ Используйте `.gitignore` для исключения чувствительных данных
- ⚠️ Регулярно обновляйте зависимости: `pip install -U -r requirements.txt`

## 📄 Лицензия

MIT License

## 📚 Дополнительная документация

- [PRD.md](PRD.md) - Полная техническая документация
- [WHISPER_SETUP.md](WHISPER_SETUP.md) - Детальная инструкция по настройке Whisper
- [DEPLOY_UBUNTU.md](DEPLOY_UBUNTU.md) - Развертывание на Ubuntu 22.04
- [features/food/README.md](features/food/README.md) - Документация дневника питания
- [GITHUB_DEPLOY_CHECKLIST.md](GITHUB_DEPLOY_CHECKLIST.md) - Чеклист для загрузки на GitHub

## 🤝 Поддержка

Полная документация в [PRD.md](PRD.md)

---

Сделано с ❤️ для удобного управления календарём и ведения дневника питания

